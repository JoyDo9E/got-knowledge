1.http和https的区别：
  
  http缺点：无状态、明文传输、队头阻塞（长连接时【kepp-alive】，通用一个TCP通道，每次只能处理一个请求，如果阻塞后续请求均不能被响应）;

2.HTTP中处理表单数据的提交：（2中不同的content-type取值：application/x-www-form-urlencode & multipart/form-data）
  1).application/x-www-form-urlencode: 数据会被“&”分割成为键值对；字符以url编码方式进行编码。{a: 1, b: 2} -> a=1&b=2 -> (最终形式)"a%3D1%26b%3D2"
  2).multipart/form-data: 多个表单数据传输，content-type还会跟上分隔符boundary。【每一个表格单元都是独立的资源表述】 上传多张图片时会用到

3.HTTP1.1解决队头阻塞问题（一发一收，一个任务队列中串行执行，如果队首任务发生阻塞，后续任务也无法执行）：
  1).并行链接，同一域名下增加并行数，可同时执行多个请求，多个长连接。chrome可同时执行6个请求；
  2).域名分片。请求不同域名，并行请求数以及长连接数可以提升更多。

4.cookie（HTTP是无状态，无连接的。本不需要记录状态，但当分段传输或业务需要存储状态时，需要用到cookie）
  1).同一个域名下发送请求，都会携带相同的cookie；
  2).服务端可以通过“Set-Cookie”来向客户端写入cookie
  3).cookie属性：
        生存周期（Expires & Max-Age），过期则删除，并不会再上传服务端；
        作用域（domain & path），通过这俩属性判断cookie是否属于当前作用域，eg path ='/'能匹配任意路径；
        安全相关：
          带上Secure则表示该cookie只能通过HTTPS协议才能传输；
          如果带上HttpOnly，则只能通过HTTP协议传输，不能通过JS访问【预防XSS攻击】;
          如果带上samesite属性，则可预防CSRF攻击；samesite: Strict / Lax / None。
        缺点：容量小（4kb）;性能缺陷（默认情况下该域名下所有请求均需携带cookie，可通过设置domain和path解决）；安全缺陷（纯文本传输，容易被截获，在不设置HttpOnly）
  
5.HTTP代理：
  

6.TLS（HTTPS协议较HTTP新增的应用层协议，用于加密http传输的内容）
  1).浏览器与服务端握手：
      

